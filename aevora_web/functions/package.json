{
  "name": "aevora-payments-functions",
  "description": "وظائف الدفع والاشتراك لتطبيق ايفورا (Tap/Moyasar/Stripe) — تحمي المفتاح السري وتكتب حالة الاشتراك في Firestore",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "private": true
}
